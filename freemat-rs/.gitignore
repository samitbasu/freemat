/target
**/*.rs.bk
Cargo.lock.bak

# Scratch files emitted by save/load/csv conformance .m tests, which run with
# cwd = workspace root (the reporter) or the crate dir (cargo test). These
# specific names are test scratch, never fixtures.
tmp.mat
test_load1.txt
test.csv
junk.dat
test.bmp
crates/fm-builtins/examples/scratch_probe.rs
crates/fm-parser/examples/scratch_pp.rs

# Help-system migration staging (regenerate with `cargo xtask migrate-docs`)
docs/migration-staging/
