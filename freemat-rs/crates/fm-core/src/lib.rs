//! `fm-core` — FreeMat-rs core value types and the `Array` container.
//!
//! Scaffold only (Stage 0). The numeric type system and the ndarray-backed
//! `Array` value (with an inline scalar fast path) arrive in Stage 1.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2 + 2, 4);
    }
}
