//! Migrated help docs for the `constants` section (help-system P7).
//! GENERATED by `cargo xtask migrate-place` — do not edit by hand.

fm_doc::register_section! {
    id: "constants",
    title: "Base Constants",
    summary: "Built-in numeric and symbolic constants.",
}

fm_doc::register_doc! {
    name: "e",
    aliases: [],
    section: "constants",
    summary: "Euler Constant (Base of Natural Logarithm)",
    body: r#"
## Usage
Returns a `double` (64-bit floating point number) value that represents Euler's constant, the base of the natural logarithm.  Typical usage

```text
   y = e
```
This value is approximately 2.718281828459045.

## Example
The following example demonstrates the use of the `e` function.

```fm-exec
e
log(e)
```
"#,
}

fm_doc::register_doc! {
    name: "eps",
    aliases: [],
    section: "constants",
    summary: "Double Precision Floating Point Relative Machine Precision Epsilon",
    body: r#"
## Usage
Returns `eps`, which quantifies the relative machine precision
of floating point numbers (a machine specific quantity).  The syntax
for `eps` is:

```text
   y = eps
   y = eps('double')
   y = eps(X)
```
First form returns `eps` for `double` precision values. For most
typical processors, this value is approximately `2^-52`, or 2.2204e-16.
Second form return `eps` for class `double` or `single`.
Third form returns distance to the next value greater than X.

## Example
The following example demonstrates the use of the `eps` function,
and one of its numerical consequences.

```fm-exec
eps
1.0+eps
eps(1000.)
```
"#,
}

fm_doc::register_doc! {
    name: "false",
    aliases: [],
    section: "constants",
    summary: "Logical False",
    body: r#"
## Usage
Returns a logical 0.  The syntax for its use is

```text
   y = false
```
You can also create an array of logical ones using
the syntax

```text
   y = false(d1,d2,...,dn)
```
or the syntax

```text
   y = false([d1,d2,...,dn])
```
"#,
}

fm_doc::register_doc! {
    name: "feps",
    aliases: [],
    section: "constants",
    summary: "Single Precision Floating Point Relative Machine Precision Epsilon",
    body: r#"
## Usage
Returns `feps`, which quantifies the relative machine precision
of floating point numbers (a machine specific quantity).  The syntax
for `feps` is:

```text
   y = feps
```
which returns `feps` for `single` precision values. For most
typical processors, this value is approximately `2^-24`, or 5.9604e-8.

## Example
The following example demonstrates the use of the `feps` function,
and one of its numerical consequences.

```fm-exec
feps
1.0f+eps
```
"#,
}

fm_doc::register_doc! {
    name: "i-j",
    aliases: [],
    section: "constants",
    summary: "Square Root of Negative One",
    body: r#"
## Usage
Returns a `complex` value that represents the square root of -1.  There are two
functions that return the same value:

```text
   y = i
```
and

```text
   y = j.
```
This allows either `i` or `j` to be used as loop indices.  The returned value is a 32-bit complex value.

## Example
The following examples demonstrate a few calculations with `i`.

```fm-exec
i
i^2
```

The same calculations with `j`:

```fm-exec
j
j^2
```

Here is an example of how `i` can be used as a loop index and then recovered as the square root of -1.

```fm-exec
accum = 0; for i=1:100; accum = accum + i; end; accum
i
clear i
i
```
"#,
}

fm_doc::register_doc! {
    name: "inf",
    aliases: [],
    section: "constants",
    summary: "Infinity Constant",
    body: r#"
## Usage
Returns a value that represents positive infinity
for both 32 and 64-bit floating point values.
There are several forms for the `Inf` function.
The first form returns a double precision `Inf`.

```text
   y = inf
```
The next form takes a class name that can be either `'double'`

```text
   y = inf('double')
```
or `'single'`:

```text
   y = inf('single')
```
With a single parameter it generates a square matrix of `inf`s.

```text
   y = inf(n)
```
Alternatively, you can specify the dimensions of the array via

```text
   y = inf(m,n,p,...)
```
or

```text
   y = inf([m,n,p,...])
```
Finally, you can add a classname of either `'single'` or `'double'`.

## Function Internals
The infinity constant has
several interesting properties.  In particular:

$$
\begin{array}{ll}
   \infty \times 0 & = \mathrm{NaN} \\                                             \infty \times a & = \infty \, \mathrm{for all} \, a > 0 \\   \infty \times a & = -\infty \, \mathrm{for all} \, a < 0 \\   \infty / \infty & = \mathrm{NaN} \\   \infty / 0 & = \infty
\end{array}
$$
Note that infinities are not preserved under type conversion to integer types (see the examples below).

## Example
The following examples demonstrate the various properties of the infinity constant.

```fm-exec
inf*0
inf*2
inf*-2
inf/inf
inf/0
inf/nan
```

Note that infinities are preserved under type conversion to floating point types (i.e., `float`, `double`, `complex` and `dcomplex` types), but not integer  types.

```fm-exec
uint32(inf)
complex(inf)
```
"#,
}

fm_doc::register_doc! {
    name: "pi",
    aliases: [],
    section: "constants",
    summary: "Constant Pi",
    body: r#"
## Usage
Returns a `double` (64-bit floating point number) value that represents pi (ratio between the circumference and diameter of a circle...).  Typical usage

```text
   y = pi
```
This value is approximately 3.141592653589793.

## Example
The following example demonstrates the use of the `pi` function.

```fm-exec
pi
cos(pi)
```
"#,
}

fm_doc::register_doc! {
    name: "teps",
    aliases: [],
    section: "constants",
    summary: "Type-based Epsilon Calculation",
    body: r#"
## Usage
Returns `eps` for double precision arguments and
`feps` for single precision arguments.  The syntax for
`teps` is

```text
   y = teps(x)
```
The `teps` function is most useful if you need to
compute epsilon based on the type of the array.

## Example
The following example demonstrates the use of the `teps` function,
and one of its numerical consequences.

```fm-exec
teps(float(3.4))
teps(complex(3.4+i*2))
```
"#,
}

fm_doc::register_doc! {
    name: "true",
    aliases: [],
    section: "constants",
    summary: "Logical TRUE",
    body: r#"
## Usage
Returns a logical 1.  The syntax for its use is

```text
   y = true
```
You can also create an array of logical ones using
the syntax

```text
   y = true(d1,d2,...,dn)
```
or the syntax

```text
   y = true([d1,d2,...,dn])
```
"#,
}
