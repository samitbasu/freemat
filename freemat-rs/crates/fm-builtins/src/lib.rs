//! `fm-builtins` — FreeMat-rs builtin functions ported from libCore.
//!
//! Scaffold only (Stage 0). Elementary math, reductions, and array manipulation
//! builtins arrive in Stages 5 and 6.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2 + 2, 4);
    }
}
