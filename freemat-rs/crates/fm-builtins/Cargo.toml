[package]
name = "fm-builtins"
description = "FreeMat-rs builtin functions ported from libCore"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
fm-core = { version = "0.0.0", path = "../fm-core" }
fm-interp = { version = "0.0.0", path = "../fm-interp" }
fm-linalg = { version = "0.0.0", path = "../fm-linalg" }
fm-parser = { version = "0.0.0", path = "../fm-parser" }
fm-graphics = { version = "0.0.0", path = "../fm-graphics" }
fm-io = { version = "0.0.0", path = "../fm-io" }
# P3 smoke fixture: the `register_doc!` macro + inventory submission for the one
# `cos` doc entry (P7 will depend on this for the full doc surface).
fm-doc = { version = "0.0.0", path = "../fm-doc" }
ndarray.workspace = true
rand.workspace = true
rand_distr.workspace = true
chrono.workspace = true
# Help system (P5): `help`/`helpwin` builtins — open the browser help page and
# detect OSC 8 hyperlink support for the clickable `Browser:` line.
webbrowser.workspace = true
supports-hyperlinks.workspace = true

[lints]
workspace = true
