[package]
name = "fm-parser"
description = "FreeMat-rs lexer, recursive-descent parser, and AST"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
miette.workspace = true
thiserror.workspace = true

[dev-dependencies]
# The library only defines `Diagnostic`s; tests opt into the graphical
# (`fancy`) renderer to assert that a malformed snippet renders annotated.
miette = { workspace = true, features = ["fancy"] }

[lints]
workspace = true
