//! `fm-parser` — FreeMat-rs lexer, recursive-descent parser, and AST.
//!
//! Scaffold only (Stage 0). The hand-written lexer, parser, span-carrying AST,
//! and `miette` diagnostics arrive in Stage 2.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2 + 2, 4);
    }
}
