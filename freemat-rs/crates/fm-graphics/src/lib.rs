//! `fm-graphics` — FreeMat-rs handle-graphics scene model and JSON wire protocol.
//!
//! Scaffold only (Stage 0). The figure/axes/series scene graph and the serde
//! protocol streamed to Plotly.js arrive in Stage 7.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2 + 2, 4);
    }
}
