//! `fm-io` — FreeMat-rs file I/O, MAT-file support, and FFT/regex-backed builtins.
//!
//! Scaffold only (Stage 0). MAT-file read/write, `save`/`load`, FFT (rustfft),
//! and regex builtins arrive in Stage 8.

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_builds() {
        assert_eq!(2 + 2, 4);
    }
}
