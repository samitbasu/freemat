[package]
name = "fm-interp"
description = "FreeMat-rs tree-walking interpreter, scopes, and builtin registry"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
fm-core = { version = "0.0.0", path = "../fm-core" }
fm-parser = { version = "0.0.0", path = "../fm-parser" }
fm-linalg = { version = "0.0.0", path = "../fm-linalg" }
fm-graphics = { version = "0.0.0", path = "../fm-graphics" }
ndarray.workspace = true
miette.workspace = true
thiserror.workspace = true
rustc-hash.workspace = true
smallvec.workspace = true

[dev-dependencies]
# Render annotated diagnostics in error-path tests.
miette = { workspace = true, features = ["fancy"] }

[lints]
workspace = true
